version: '3.8'

services:
  car-price-api:
    build: .
    ports:
      - "8001:8000"
    volumes:
      - ./data:/app/data
      - ./models:/app/models
    command: uvicorn src.api.app:app --host 0.0.0.0 --port 8000